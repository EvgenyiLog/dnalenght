# dnalenght
```markdown
# Проект анализа и визуализации данных

Этот проект предназначен для анализа данных, обработки сигналов и визуализации результатов с использованием Python, Web и специализированных библиотек.

---

## 📦 Требования

- Python 3.13 или 3.14
- Веб-браузер для фронтенд-секции
- Git (опционально для клонирования репозитория)

---

## ⚡ Установка Python

### 1. С официального сайта Python

1. Перейти на [python.org](https://www.python.org/downloads/)
2. Скачать и установить Python 3.13 или 3.14
3. Проверить установку:

```bash
python --version
pip --version
```

---

### 2. Через pyenv (рекомендовано для Linux/Mac)

```bash
# Установить pyenv
curl https://pyenv.run | bash

# Добавить в ~/.bashrc или ~/.zshrc
export PATH="$HOME/.pyenv/bin:$PATH"
eval "$(pyenv init --path)"
eval "$(pyenv virtualenv-init -)"

# Установить нужную версию Python
pyenv install 3.13.12
pyenv install 3.14.0

# Создать окружение
pyenv virtualenv 3.13.12 myproject-env
pyenv activate myproject-env
```

---

## 🧰 Установка зависимостей через UV

Создаем файл `uv.toml` и указываем библиотеки:

```toml
[project]
name = "myproject"
version = "0.1.0"

[dependencies]
numpy = "*"
scipy = "*"
matplotlib = "*"
seaborn = "*"
pandas = "*"
pyyaml = "*"

[web]
chartjs = "*"
jspdf = "*"
chart-annotation = "*"

[optional]
pyyawt = { version = "*", platform = "windows" }
```

Установка:

```bash
uv add -group dependencies
uv add -group web
uv add -group optional
```

> ⚠️ `pyyawt` опционально только под Windows для wden.

---

## 📂 Структура проекта

```
myproject/
│
├─ src/
│   ├─ analysis.py
│   ├─ utils.py
│
├─ static/
│   ├─ index.html
│   ├─ js/
│   │   ├─ chart.js
│   │   ├─ jspdf.min.js
│   │   └─ chart-annotation.min.js
│   └─ css/
│       └─ styles.css
│
├─ uv.toml
├─ README.md
└─ requirements.txt
```

---

## 🛠 Python-библиотеки

- `numpy`, `scipy` – обработка сигналов, вычисления
- `matplotlib`, `seaborn` – визуализация графиков
- `pandas` – работа с таблицами
- `pyyaml` – работа с конфигами
- `pyyawt` – TQWT и wden (опционально, Windows)

Установка через pip (если не используешь uv):

```bash
pip install numpy scipy matplotlib seaborn pandas pyyaml
# для Windows, если нужен wden
pip install pyyawt
```

---

## 🌐 Веб-библиотеки

Для фронтенда:

- `Chart.js` – графики
- `Chart Annotation` – аннотации на графиках
- `jsPDF` – экспорт графиков в PDF

Пример подключения в `index.html`:

```html
<script src="js/chart.js"></script>
<script src="js/chart-annotation.min.js"></script>
<script src="js/jspdf.min.js"></script>
```

---

## 🚀 Запуск проекта

### Python backend

```bash
# Запуск анализа
python src/analysis.py
```

### Фронтенд

Открой `static/index.html` в браузере и убедись, что JS библиотеки подключены.

---

## 📑 Примечания

- Используемые версии Python: 3.13/3.14
- UV позволяет изолировать окружение и управлять группами зависимостей
- Pyyawt нужен только для Windows и для функционала wden
- Chart.js и jspdf используются для визуализации и экспорта графиков

---

## 📖 Дополнительно

- Для настройки UV окружения можно использовать `uv.toml` с разделением зависимостей на группы
- Для Windows рекомендуется опционально подключать pyyawt
- Frontend можно расширять любыми JS библиотеками через `static/js/`

---

## 📝 Контакты

- Автор: [Your Name]
- Email: your.email@example.com
- GitHub: https://github.com/yourusername/myproject
```

